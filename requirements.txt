# Core Dependencies

# Data Processing
pandas==2.1.3
numpy==1.26.2
scikit-learn==1.3.2

# Text Processing & NLP
nltk==3.8.1
sentence-transformers==2.2.2
transformers==4.35.2
tokenizers==0.14.1

# LLM & RAG Framework
# Checkpoint 3 uses langchain-core interfaces (Embeddings, BaseRetriever) and
# langchain-text-splitters. Verified against langchain 1.3.18 / core 1.6.1;
# the 1.x API differs substantially from 0.1.x.
langchain>=0.1.0
langchain-community>=0.0.10
langchain-openai>=0.0.5
llama-index>=0.9.0
openai>=1.0.0

# Vector Database
# Lower bound rather than a pin: the VectorStore uses the PersistentClient API,
# which is stable across 0.4.x and 1.x. Verified against chromadb 1.5.9.
chromadb>=0.4.15
faiss-cpu==1.7.4

# API & Interface
streamlit==1.29.0
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Deployment & Configuration
python-dotenv==1.0.0
docker==6.1.0

# Fine-tuning
peft==0.7.1
bitsandbytes==0.41.2

# Utilities
loguru==0.7.2
tqdm==4.66.1
python-dateutil==2.8.2
